# 🔄 FLUJO DE DATOS - CÓMO FUNCIONA AHORA

## 📊 Diagrama de migración automática

```
┌─────────────────────────────────────────────────────────┐
│                  PÁGINA INICIA (DOMContentLoaded)       │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
        ┌──────────────────────────────┐
        │  ¿Es formulario público?     │
        │  (URL tiene parámetro leader)│
        └──────────┬────────┬──────────┘
                   │        │
              NO  │        │  SÍ
                   ▼        ▼
            ┌──────────┐  ┌──────────────────┐
            │Migración │  │Mostrar formulario│
            │automática│  │de registro       │
            └────┬─────┘  └──────────────────┘
                 │
                 ▼
        ┌──────────────────────────┐
        │  POST /api/migrate       │
        │  (convierte data.json)   │
        └────┬─────────────────────┘
             │
             ▼
        ┌─────────────────────────────────────┐
        │  MongoDB recibe datos convertidos:  │
        │  - leaders (4)                      │
        │  - registrations (1)                │
        └────┬────────────────────────────────┘
             │
             ▼
        ┌──────────────────────────┐
        │  loadLeaders()           │
        │  loadRegistrations()     │
        │  updateDashboard()       │
        └────┬─────────────────────┘
             │
             ▼
        ┌──────────────────────────────┐
        │  Dashboard actualizado      │
        │  ✅ Datos visibles          │
        └──────────────────────────────┘
```

---

## 📋 Diagrama: Crear nuevo líder

```
┌─────────────────────────────────┐
│  Usuario hace clic en           │
│  "Nuevo Líder"                  │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Modal se abre                  │
│  (Formulario vacío)             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Usuario completa campos:       │
│  - Nombre                       │
│  - Email                        │
│  - Teléfono                     │
│  - Área                         │
│  - Activo (checkbox)            │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Usuario hace clic en           │
│  "Guardar Líder"                │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│  saveLeader() valida campos                  │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  POST /api/leaders (con eventId del evento) │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  MongoDB crea el líder                      │
│  - _id generado automáticamente             │
│  - token generado automáticamente           │
│  - eventId asociado                         │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  Response 200 con nuevo líder                │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  JavaScript:                                 │
│  - showToast('success', 'Líder guardado')  │
│  - Modal se cierra                          │
│  - Formulario se limpia                     │
│  - loadLeaders() recarga tabla              │
│  - updateDashboard() actualiza tarjetas    │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  ✅ Nuevo líder visible en tabla            │
│  ✅ Dashboard actualizado                   │
│  ✅ Usuario ve confirmación                 │
└──────────────────────────────────────────────┘
```

---

## 📋 Diagrama: Crear nuevo registro en evento

```
┌─────────────────────────────────┐
│  Usuario selecciona un evento   │
│  (desde página de inicio)       │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  activeEventId se guarda        │
│  localStorage.setItem()         │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Dashboard se carga             │
│  (eventId ya está activo)       │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Usuario hace clic en           │
│  "Nuevo Registro"               │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Modal de registro se abre      │
│  - Selector de líderes se llena │
│    (líderes del evento actual)  │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Usuario completa:              │
│  - Nombre / Apellido            │
│  - Cédula                       │
│  - Email                        │
│  - Teléfono                     │
│  - Selecciona Líder             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Usuario hace clic en           │
│  "Guardar Registro"             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│  saveAdminRegistration() crea objeto:       │
│  {                                          │
│    firstName, lastName, cedula,             │
│    email, phone,                            │
│    leaderId (del selector),                 │
│    eventId (activeEventId) ← IMPORTANTE    │
│  }                                          │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  POST /api/registrations con eventId        │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  MongoDB crea registro:                      │
│  - _id generado automáticamente             │
│  - leaderId: del líder seleccionado        │
│  - eventId: del evento actual              │
│  - date: fecha/hora actual                 │
│  - confirmed: false (por defecto)          │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  Response 200 con nuevo registro            │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  JavaScript:                                 │
│  - Modal se cierra                          │
│  - Formulario se limpia                     │
│  - loadLeaders() recarga líderes           │
│  - loadRegistrations() recarga registros   │
│  - updateDashboard() actualiza tarjetas    │
└────┬───────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  ✅ Nuevo registro visible en tabla         │
│  ✅ Contador de personas actualizado        │
│  ✅ El registro está asociado al evento    │
└──────────────────────────────────────────────┘
```

---

## 🔗 Flujo de datos: Backend

```
USUARIO FRONTEND
       │
       ├─ POST /api/leaders ─────────┐
       │                              │
       ├─ POST /api/registrations ───┤
       │                              │
       ├─ GET /api/leaders ──────────┤
       │                              │
       └─ GET /api/registrations ────┤
                                      ▼
                              EXPRESS SERVER
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
            MONGOOSE              VALIDATION        NOTIFICATIONS
                 │                    │                   │
                 │              • Validar                • Resend
                 │              • Normalizar            • Twilio
                 ▼              • Completar            • WhatsApp
            MONGODB                   │                   │
                 │                    └───────┬──────────┘
            GUARDAR                           │
                 │                            ▼
                 │                      NOTIFICACIONES
                 ▼                            │
            RESPONDER                         │
            JSON 200                          ▼
                 │                      ✅ Email/SMS
                 └────────────────────────────┘
                        │
                        ▼
                  FRONTEND RECIBE
                  - Actualiza tabla
                  - Cierra modal
                  - Muestra notificación
```

---

## 📍 Dónde se asocia el eventId

### En Líderes:
```javascript
// En saveLeader()
const leaderData = {
  name, email, phone, area, active,
  eventId: activeEventId  // ← Se incluye aquí
};

await fetch('/api/leaders', {
  method: 'POST',
  body: JSON.stringify(leaderData)
});
```

### En Registros:
```javascript
// En saveAdminRegistration()
const reg = {
  firstName, lastName, cedula, email, phone,
  leaderId,
  eventId: activeEventId  // ← Se incluye aquí
};

await fetch('/api/registrations', {
  method: 'POST',
  body: JSON.stringify(reg)
});
```

### En Consultas:
```javascript
// GET con filtro por evento
const url = `/api/leaders?eventId=${activeEventId}`;
const url = `/api/registrations?eventId=${activeEventId}`;
```

---

## ✅ Checklist: Qué cambió

- [x] Migración automática de `data.json` → MongoDB
- [x] Crear líder actualiza tabla automáticamente
- [x] Crear registro actualiza tabla automáticamente
- [x] Líderes se asocian a eventos
- [x] Registros se asocian a eventos
- [x] Modales se cierran correctamente
- [x] Formularios se limpian después de guardar
- [x] Notificaciones visuales mejoradas
- [x] Mejor manejo de errores
- [x] Logs en consola para debugging

---

¡Así es como funciona todo ahora! 🎉

